<p id="notice"><%= notice %></p>

<h1>Tipo Entidades</h1>

<table>
  <thead>
    <tr>
      <th>Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_entidades.each do |tipo_entidad| %>
      <tr>
        <td><%= tipo_entidad.tipo %></td>
        <td><%= link_to 'Show', tipo_entidad %></td>
        <td><%= link_to 'Edit', edit_tipo_entidad_path(tipo_entidad) %></td>
        <td><%= link_to 'Destroy', tipo_entidad, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo Entidad', new_tipo_entidad_path %>
